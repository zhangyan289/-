# 项目交接总结：线上自习室

> 生成时间：2026-07-21
> 当前会话末尾任务：集成 bg6 背景、待实现喂养线条小狗机制

---

## 1. 项目概述
- **项目名称**：线上自习室（情侣/双人线上自习场景）
- **功能定位**：双人在线自习、番茄钟、倒计时、音乐入口、动态背景、小狗热区互动、在线状态显示、待办清单（TodoList）。
- **当前状态**：主要功能已可用，正在做第 6 场景背景和 TodoList 的喂养小狗机制。

---

## 2. 技术栈
- **前端**：Vue 3 + Vite + Options API（`*.vue` 单文件组件）
- **后端**：Node.js + Express + better-sqlite3
- **样式**：纯 CSS / 自定义 CSS 变量，少量图标/图片素材
- **构建**：前端 `npm run build` / `npm run dev`；后端 `node src/index.js`
- **启动入口**：本地用 `start.py`（启动后端 + 打开浏览器）
- **数据库**：`server/data/app.sqlite`（SQLite）
- **密码哈希**：bcryptjs

---

## 3. 目录结构（关键路径）
```
线上自习室/
├── client/
│   ├── src/
│   │   ├── pages/RoomPage.vue          # 主房间页（核心页面）
│   │   ├── components/
│   │   │   ├── AnimatedBackground.vue  # 背景、特效、热区、场景切换
│   │   │   ├── TodoList.vue            # 待办清单组件
│   │   │   └── ...                     # 其它小组件
│   │   └── App.vue / main.js
│   └── public/assets/user/             # 用户/背景素材
│       ├── bg{n}.png/jpg/jpeg          # 背景图（n=1~6）
│       ├── dogsLeft_hotspots{n}.svg    # 左侧小狗热区蒙版
│       ├── dogsRight_hotspots{n}.svg   # 右侧小狗热区蒙版
│       └── fx_scene{n}.webm/mp4        # 场景特效透明视频（可选）
├── server/
│   ├── src/index.js                    # 后端主入口（Express 路由）
│   ├── data/app.sqlite                 # 数据库
│   └── .env                            # 环境变量（已创建，本地密码）
└── start.py                            # 本地一键启动脚本
```

---

## 4. 已完成功能

### 4.1 番茄钟增强
- **文件**：`client/src/pages/RoomPage.vue`
- **改动**：
  - 新增“清零次数”按钮，重置番茄钟时间的同时重置完成次数。
  - 修复了“开始之后不能清零”的问题。
- **注意事项**：按钮状态与 `isRunning`、`completed` 等字段联动，后续改计时逻辑时要同步检查清零逻辑。

### 4.2 在线状态实时化
- **文件**：`client/src/pages/RoomPage.vue`
- **改动**：
  - 轮询间隔从 10 秒缩短到 5 秒。
  - 增加 `visibilitychange` 页面可见性刷新。
  - 增加用户交互（点击、触摸）节流刷新，5 秒内最多触发一次。
- **注意事项**：注意避免请求风暴，后续若加 WebSocket 可替换轮询。

### 4.3 平板/移动端适配
- **文件**：`client/src/pages/RoomPage.vue` + CSS
- **改动**：
  - 增加响应式 CSS，适配平板竖屏/横屏。
  - 优化触控按钮大小、间距、字体缩放。
  - 图片热区支持 pointer 事件。
- **注意事项**：女友使用平板登录，后续改 UI 要优先验证 768px~1024px 区间。

### 4.4 前端稳定性增强
- **文件**：`client/src/pages/RoomPage.vue`
- **改动**：统一为 API 请求添加 `fetchWithTimeout` 超时处理。
- **注意事项**：超时时间统一配置，避免默认 fetch 在网络差时挂死。

### 4.5 后端代码清理
- **文件**：`server/src/index.js`
- **改动**：修复 `/api/study/history` 路由中 `sendHistory` 函数重复定义问题。
- **注意事项**：后续合并代码时要检查 Express 路由是否重复挂载。

### 4.6 本地登录问题修复
- **文件**：`server/.env`、数据库 `server/data/app.sqlite`
- **改动**：
  - 创建 `server/.env`（若生产环境缺失会导致密码验证失败）。
  - 修复两个账号的本地密码 hash：
    - 小鸡毛：`hwy041203`
    - 小白：`zy041016`
- **注意事项**：生产环境（Gitee 部署）的密码 hash 可能仍需要手动同步，本地修改不影响远程数据库。

---

## 5. 当前进行中的任务

### 5.1 第 6 场景背景集成（bg6）
- **用户素材**：
  - 工作区根目录有 `bg6.png`、`bg6.svg`（用户称“对应的蒙版”）。
  - 但代码约定蒙版文件名为 `dogsLeft_hotspots6.svg` / `dogsRight_hotspots6.svg`，需要确认并移动/重命名。
- **需要修改的文件**：
  - `client/src/components/AnimatedBackground.vue`
    - `SCENE_COUNT` 从 `5` 改为 `6`。
    - `pickSceneFxTypes` 增加 `case 6`，配置合适的沉浸式特效（如萤火虫、微光、薄雾、花瓣等）。
    - 确保背景图使用 `.bg-sway` 保持“微微摇晃”。
  - `client/src/pages/RoomPage.vue`
    - 场景切换按钮显示从 `场景：{{ bgSceneId }}/5` 改为 `/6`。
- **素材目标目录**：`client/public/assets/user/`

### 5.2 TodoList 喂养线条小狗机制（尚未开始）
- **需求**：按照任务完成获得积分，然后喂养线条小狗。
- **待确认**：小狗素材、积分规则、喂养交互方式、是否后端持久化积分。
- **建议后续动作**：先确认素材（如静态图/动图/Canvas），再设计积分表和 API。

---

## 6. 关键代码约定

### 6.1 背景系统（AnimatedBackground.vue）
- `sceneId` 从 1 开始编号。
- `SCENE_COUNT` 控制总场景数。
- 背景图候选：`getSceneBgCandidates(sceneId)` 返回 `['/assets/user/bg{n}.png', ...]`。
- 热点蒙版：`getHotspotUrlsForScene(sceneId)` 返回 `['/assets/user/dogsLeft_hotspots{n}.svg', '/assets/user/dogsRight_hotspots{n}.svg']`。
- 特效分配：`pickSceneFxTypes(sceneId)` 返回字符串数组，如 `['motes']`、`['meteors','fireflies']`、`['butterflies']`、`['glints']`、`['dandelion']`。
- “微微摇晃”：通过 CSS 类 `.bg-sway` 的 sway 动画实现。

### 6.2 特效类型（Canvas 实现）
- `motes`：浮尘
- `meteors` / `fireflies`：流星萤火虫
- `butterflies`：蝴蝶
- `glints`：光斑
- `dandelion`：蒲公英
- 场景 6 建议根据 `bg6.png` 风格选择：暖色调配萤火虫/微光；清新色调配花瓣/薄雾；梦幻色调配光斑/蒲公英。

### 6.3 场景切换按钮
- 位置：RoomPage.vue 左上角/相关控制区。
- 显示：`场景：{{ bgSceneId }}/N`，N 需与 `SCENE_COUNT` 一致。

### 6.4 用户登录
- 后端读取 `server/.env` 中的 `JWT_SECRET` 等。
- 数据库 `users` 表 `password_hash` 为 bcrypt hash。
- 本地账号：小鸡毛 / 小白。

---

## 7. 已知问题与注意事项

1. **bg6 素材位置不对**：用户文件在根目录，需移动到 `client/public/assets/user/`。
2. **蒙版文件名需确认**：用户叫 `bg6.svg`，但代码期望 `dogsLeft_hotspots6.svg` / `dogsRight_hotspots6.svg`。需确认用途后重命名或改代码。
3. **生产环境密码**：本地已改，但 Gitee 线上数据库可能仍不同，需单独检查。
4. **特效视频优先级**：`AnimatedBackground.vue` 优先使用用户自定义 `fx_sceneN.webm/mp4`，否则用 Canvas 特效。若加新素材，注意先放视频，否则 Canvas 兜底。
5. **在线状态后续**：目前是轮询，体验可进一步提升，后续可考虑 WebSocket 或 SSE。

---

## 8. 下一步建议

1. 移动/重命名 `bg6.png` 和 `bg6.svg` 到 `client/public/assets/user/`。
2. 修改 `AnimatedBackground.vue`：
   - `SCENE_COUNT = 6`
   - 增加 `case 6` 的特效配置
   - 确认 `.bg-sway` 类应用于 bg6 背景图
3. 修改 `RoomPage.vue`：场景按钮显示 `/6`。
4. 构建并本地验证 `start.py` 登录和场景切换。
5. 向用户确认喂养线条小狗的素材和规则，然后设计后端积分表/前端交互。

---

## 9. 常用命令
- 本地启动：`python start.py`
- 前端开发：`cd client && npm run dev`
- 后端开发：`cd server && node src/index.js`
- 前端构建：`cd client && npm run build`
- 数据库查看：使用任意 SQLite 浏览器打开 `server/data/app.sqlite`

---

## 10. 备注
- 所有改动已推送到 Gitee，下一次工作前可先 `git pull` 同步。
- 若新对话继续，优先把 `bg6.svg` 的用途确认清楚，再决定是否沿用 `dogsLeft_hotspots6.svg` / `dogsRight_hotspots6.svg` 的命名约定。
