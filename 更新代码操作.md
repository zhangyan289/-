太好了！你改了代码，现在要**把最新代码更新到服务器上**。  
✅ **不需要重装、不需要删文件、不会丢数据** —— 只需几步安全操作。

---

## 🔄 服务器代码更新全流程（日常开发推荐）

> 假设你本地已 `git commit` 并 `git push` 到远程仓库（如 GitHub/Gitee）

### ✅ 步骤 1：登录服务器（SSH）
```bash
ssh root@你的服务器IP
```

---

### ✅ 步骤 2：进入项目目录
```bash
cd /opt/app
```

---

### ✅ 步骤 3：拉取最新代码
```bash
git pull origin main
```
> 🔁 如果你用的是 `master` 或其他分支，改成对应名字，比如：
> ```bash
> git pull origin master
> ```

> 💡 如果提示冲突（conflict），说明你改了服务器上的文件（不推荐）。  
> **最佳实践：服务器只部署，不直接改代码！**

---

### ✅ 步骤 4：【仅当依赖有变化时】重装依赖
- 如果你改了 `package.json`（比如加了新库），运行：
  ```bash
  npm install
  ```
- 如果只是改了业务逻辑（`.js` / `.vue` 文件），**跳过这步！**

> ⚠️ 特别注意：如果 `better-sqlite3` 或其他 native 模块升级了，必须 `npm install` 重新编译！

---

### ✅ 步骤 5：重新构建前端（必须做！）
```bash
npm run build -w client
```
> 🎨 这会把最新的 Vue/React 代码打包成 `dist/` 目录下的静态文件，Nginx 才能提供新页面。

---

### ✅ 步骤 6：重启后端服务（加载新逻辑）
```bash
pm2 restart online-study-room
```
> 🔁 这会让 Node.js 重新加载 `/server` 下的新代码，API 接口立即生效。

---

### ✅ 步骤 7：验证更新成功

#### 检查后端：
```bash
curl http://127.0.0.1:3000/api/health
# 应返回 {"ok":true}
```

#### 检查前端：
- 打开浏览器，访问 `http://你的公网IP`
- **强制刷新**（Ctrl+F5 或 Cmd+Shift+R）清除缓存
- 看是否看到你改的新功能/界面

---

## 🛠 一键更新脚本（可选，超方便）

创建 `/opt/app/update.sh`：
```bash
#!/bin/bash
cd /opt/app || exit 1

echo "🔄 拉取最新代码..."
git pull

echo "📦 安装依赖（如有更新）..."
npm install

echo "🎨 构建前端..."
npm run build -w client

echo "🔁 重启后端..."
pm2 restart online-study-room

echo "✅ 更新完成！当前时间: $(date)"
```

赋权并运行：
```bash
chmod +x /opt/app/update.sh
./update.sh
```

以后更新只需一条命令 👍

---

## ❌ 不需要做的操作（避免踩坑）

| 操作 | 为什么不用做 |
|------|-------------|
| 删除 `/opt/app` | 会丢失配置和构建产物 |
| 重配 `.env` | 密码和 JWT_SECRET 不能变 |
| 重建数据库 | 用户数据会清空！ |
| 重配 Nginx | 配置没变，无需动 |

---

## ✅ 总结：日常更新就这 4 步

```bash
cd /opt/app
git pull
npm run build -w client
pm2 restart online-study-room
```

> 💡 **记住：只动代码，不动状态（.env + 数据库） = 安全更新！**

你现在就可以执行这个流程，5 分钟内让新功能上线！🎉  
需要我帮你检查当前 Git 分支或是否有未提交的改动吗？

密码！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！

当然！你需要记住的 三个关键密码/密钥如下（它们都来自你的 .env 文件）：

🔑 1. JWT_SECRET（最重要！）
作用：用于签发和验证用户登录 Token，一旦改变，所有已登录用户会立即失效。
格式：一串 64 位的长随机字符串，例如：
7a3b9c1d5e8f2a4b6c0d1e3f5a7b9c1d5e8f2a4b6c0d1e3f5a7b9c1d5e8f2a4b
✅ 必须备份！建议存到密码管理器（如 Bitwarden、1Password）或加密笔记中。

👤 2. PRESET_USER_A_PASSWORD（管理员账号密码）
作用：对应前端“小鸡毛”账号的登录密码。
你当前设置的是：hwy041203
✅ 必须记住，否则无法登录管理员账户。

👤 3. PRESET_USER_B_PASSWORD（普通用户账号密码）
作用：对应前端“小白”账号的登录密码。
你当前设置的是：zy041016
✅ 必须记住，否则无法用第二个账号登录。

📌 总结（请立即保存！）

名称 值（以你当前配置为准） 用途
------ ------------------------ ------
JWT_SECRET 7a3b9c1d5e8f2a4b6c0d1e3f5a7b9c1d5e8f2a4b6c0d1e3f5a7b9c1d5e8f2a4b Token 签名密钥（绝对不能丢）
PRESET_USER_A_PASSWORD hwy041203 小鸡毛账号密码
PRESET_USER_B_PASSWORD zy041016 小白账号密码
💡 这三个值都在 /opt/app/server/.env 中，重装或更新代码时务必保留！

你可以现在就复制保存到安全的地方。需要我帮你生成一个带注释的备份文件吗？