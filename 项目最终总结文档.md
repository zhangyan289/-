# 线上自习室 — 项目负责人最终文档

> 本文档包含项目全部关键信息、凭证与部署说明，仅限项目负责人保管，请勿外传。
> 生成时间：2026-07-23
> 最近更新：本次会话结束（学习计时刷新不丢、桌宠答题去英语、积分规则最终化）

---

## 1. 项目基本信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 线上自习室（情侣/双人线上自习室） |
| 项目定位 | 双人在线自习、番茄钟、倒计时、Todo List、动态背景、桌面宠物、英语 PK、大模型出题便利贴 |
| 本地工作区 | `C:/Users/zhngyan/Desktop/线上自习室` |
| 线上部署路径 | `/opt/app`（阿里云 ECS） |
| 公网访问地址 | http://120.55.183.89 |
| 域名 | 暂无（使用公网 IP 直接访问） |
| 进程管理 | PM2（进程名 `online-study-room`） |

---

## 2. 账号与凭证

### 2.1 云服务器

| 项 | 值 |
|----|-----|
| 公网 IP | 120.55.183.89 |
| 服务商 | 阿里云 ECS |
| 实例名 | iZbp12nrfn438sb6216l9gZ |
| 地域 | 华东 1（杭州） |
| 系统 | Alibaba Cloud Linux 3.2104 LTS 64位 |
| 登录用户 | root |
| root 密码 | `tbszxy123!` |
| 私网 IP | 172.19.175.154 |
| 安全组 | sg-bp1glse45skiyu4lltib |
| 已开放端口 | 80（HTTP，任何来源）、22（SSH，指定来源） |
| 项目部署目录 | `/opt/app` |
| 数据库路径 | `/var/lib/online-study-room/app.sqlite` |
| PM2 日志 | `/root/.pm2/logs/` |

> ⚠️ 重要：root 密码和 Kimi API Key 曾在某次提交中误提交到 Gitee 仓库（commit 4f928d7），虽已删除文件，但 Git 历史仍可搜到。建议尽快到阿里云控制台重置 root 密码，并在 Kimi 开放平台重新生成 API Key 替换。

### 2.2 网站登录账号

| 用户名 | 密码 | 对应角色 | 备注 |
|--------|------|---------|------|
| 小鸡毛 | `hwy041203` | 小金毛桌宠 / 小金毛 Todo / 小金毛便利贴 | 计算机科学与技术（408） |
| 小白 | `zy041016` | 小白桌宠 / 小白 Todo / 小白便利贴 | 电子信息 0854（自控+现控），英语一/数学一 |
| userA | `hwy041203` | 旧用户名，首次登录会自动迁移到“小鸡毛” | 兼容旧账号 |
| userB | `zy041016` | 旧用户名，首次登录会自动迁移到“小白” | 兼容旧账号 |

> 密码同时配置在 `/opt/app/server/.env` 中：`PRESET_USER_A_PASSWORD`、`PRESET_USER_B_PASSWORD`。

### 2.3 Kimi 大模型 API

| 项 | 值 |
|----|-----|
| 环境变量名 | `KIMI_API_KEY` |
| 当前 Key | `sk-Gcn31mrc9sTZkrQt9mEun0lOiEwiMT1ZaGglym25KwDVSY0t` |
| 配置路径 | `/opt/app/server/.env` |
| 服务商 | Moonshot（Kimi） |
| 基础地址 | `https://api.moonshot.cn/v1` |
| 用途 | 生成便利贴、英语单词 PK、桌宠每日一题（数学/专业课） |

> 该 Key 已充值，若后续提示额度不足或欠费，需到 Kimi 开放平台重新充值并替换。

### 2.4 Git 仓库

| 仓库 | 地址 | 用途 |
|------|------|------|
| Gitee（主仓库） | https://gitee.com/zhangyanBM/online-self-study-room.git | 生产部署来源，服务器 `origin` 指向此处 |
| GitHub（备用） | https://github.com/zhangyan289/-.git | 备用 |
| 当前分支 | `main` | — |

---

## 3. 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + Vite + Vue Router（组合式 API） |
| 后端 | Node.js + Express |
| 数据库 | SQLite（better-sqlite3） |
| 认证 | JWT + bcryptjs |
| 进程管理 | PM2 |
| 反向代理 | 阿里云 ECS 上 80 端口直连（Express 托管 `client/dist`） |
| 构建工具 | npm workspaces：`npm run build` |
| 本地启动 | `python start.py`（启动后端 + 浏览器） |
| 远程部署 | 本地 `.deploy-now.py` 通过 `git bundle` + Paramiko 上传到服务器（因服务器 Gitee HTTPS 凭证失效） |

---

## 4. 目录结构

```
线上自习室/
├── client/                         # 前端
│   ├── src/
│   │   ├── pages/RoomPage.vue      # 主房间页（核心）
│   │   ├── components/
│   │   │   ├── AnimatedBackground.vue   # 动态背景、特效、热区
│   │   │   ├── DesktopPet.vue      # 桌宠（可拖动、投喂、答题）
│   │   │   ├── PetStatusPanel.vue  # 桌宠状态面板
│   │   │   ├── PetQuizModal.vue    # 桌宠每日一题
│   │   │   ├── EnglishQuizModal.vue# 英语单词 PK
│   │   │   ├── StickyNotes.vue     # 便利贴
│   │   │   └── ...
│   │   ├── App.vue
│   │   └── main.js
│   ├── public/assets/user/         # 用户素材（背景、蒙版、桌宠 GIF、音效等）
│   └── dist/                       # 构建产物（.gitignore，服务器本地构建）
├── server/
│   ├── src/index.js                # 后端主入口
│   ├── src/db/database.js          # 数据库初始化与迁移
│   ├── src/db/seed.js              # 预设用户与初始数据
│   ├── src/middleware/auth.js      # JWT 认证中间件
│   ├── .env                        # 环境变量（服务器上有，本地 .env.example）
│   └── package.json
├── scripts/                        # 数据迁移/检查脚本
├── 小白桌宠素材/                    # 原始桌宠 GIF（未使用，已整理到 public/assets）
├── 小鸡毛桌宠素材/                  # 原始桌宠 GIF（未使用）
├── start.py                        # 本地一键启动
├── .deploy-now.py                  # 生产部署脚本（本地，未入仓库，含服务器密码）
├── .deploy-venv/                   # 部署脚本 Python 虚拟环境（Paramiko）
├── 项目交接总结.md                  # 早期交接文档
├── 更新代码操作.md                  # 更新操作说明
└── 项目最终总结文档.md              # 本文档
```

---

## 5. 当前功能清单

### 5.1 核心自习功能
- 双人在线状态显示（轮询 1 秒阈值）
- 番茄钟（25/5，4 个后长休，可暂停/重置/清零）
- 学习计时器：点开始计时就持续累计，点暂停才停止；刷新/关闭页面不丢时长；跨零点自动分割两日
- 学习积分：每学满 1 小时加 1 积分（10 分钟 = 0 分，60 分钟 = 1 分）
- 考研倒计时 + 自定义倒计时（两人共享编辑）
- 学习记录弹窗（日历热力图 + 统计摘要）
- 共享留言箱（带未读红点）
- 音乐快捷入口（跳转网易云/QQ/汽水/酷狗/酷我）

### 5.2 动态背景系统
- 11 个场景（bg1 ~ bg11）
- 支持 `.png/.jpg/.jpeg` 背景图
- 支持 `fx_sceneN.mp4/webm` 透明 overlay 视频
- 每个场景 2~3 个 Canvas 特效（根据画面内容匹配）
- 背景微微摇晃（`.bg-sway` / `.bg-stage` 呼吸动画）
- 小狗热区蒙版：左狗 `dogsLeft_hotspotsN.svg`、右狗 `dogsRight_hotspotsN.svg`
- 场景切换按钮：`场景：{{ bgSceneId }}/11`

### 5.3 背景特效分配

| 场景 | 特效（最多 3 层） | 说明 |
|------|------------------|------|
| bg1 | motes + bokeh | 春/花瓣：浮尘 + 柔光 |
| bg2 | meteors + fireflies | 夜空：流星 + 萤火虫 |
| bg3 | butterflies + motes | 花园：蝴蝶 + 浮尘 |
| bg4 | glints + bokeh | 海面：闪光 + 柔光，小船遮罩 |
| bg5 | dandelion + motes | 草地：蒲公英 + 浮尘 |
| bg6 | fireflies + sparks + bokeh | 露营篝火 |
| bg7 | fireflies + sparks + leaves | 森林篝火 |
| bg8 | butterflies + bokeh | 花田（蝴蝶已减量） |
| bg9 | birds + motes | 地中海小镇：海鸥 + 浮尘（去掉蝴蝶） |
| bg10 | bubbles + bokeh | 水上乐园：气泡 + 柔光（已减少） |
| bg11 | leaves + bokeh + motes | 秋日农场 |

> 切场景时会先清空旧 overlay 与 Canvas，避免串场景。

### 5.4 桌宠系统
- 两只可拖动桌宠：小白（右/白色）、小金毛/小鸡毛（左/金色）
- 点击桌宠打开状态面板，可投喂、查看愉悦度、做题、进入 Todo
- 愉悦度 0~100，每 20 分一档，共 5 档
- 每小时自然衰减 5 点愉悦度
- 愉悦度 0 后 3 小时未投喂则离家出走
- 离家出走后点击可唤回，愉悦度回到 20，并给对应主人增加“不负责任”标记
- 喂食有对应 GIF，持续 30 分钟后回到当前愉悦度 GIF
- 愉悦度 GIF 每小时在该等级内随机轮播

### 5.5 积分与喂食
- 学习：每满 1 小时 +1 积分
- 完成 Todo：+5 积分
- 喂食扣自己的积分，不管喂哪只宠物
- 食物与积分（1 积分 = 5 愉悦度）：

| 食物 | 成本 | 愉悦度 | 小白专属 | 小金毛专属 | 通用 |
|------|------|--------|----------|------------|------|
| 麻辣王子 | 1 | 5 | ✅ | ❌ | — |
| 淋浴水 | 1 | 5 | ❌ | ✅ | — |
| 苹果 | 3 | 15 | ✅ | ✅ | ✅ |
| 冰淇淋 | 3 | 15 | ✅ | ✅ | ✅ |
| KFC狗粮 | 3 | 15 | ❌ | ✅ | — |
| 奶茶 | 4 | 20 | ✅ | ✅ | ✅ |
| 咖啡 | 4 | 20 | ❌ | ✅ | — |
| 米线 | 5 | 25 | ✅ | ❌ | — |
| 小蛋糕 | 6 | 30 | ✅ | ✅ | ✅ |
| 西餐 | 6 | 30 | ✅ | ❌ | — |
| 狗狗堡 | 6 | 30 | ❌ | ✅ | — |

### 5.6 桌宠答题（每日一题）
- 小金毛：数学一 + 408（数据结构、计组、计网、操作系统随机一门）
- 小白：数学一 + 自动控制原理 / 现代控制理论随机一门
- 答对 +10 愉悦度，答错 -10 愉悦度
- 冷却时间：**2 小时**，状态持久化到数据库，服务器重启不会丢
- 答完题后显示鼓励文案（从文案库随机），直到下次出题才更换
- 大模型额度不足时仅显示“不可用”，不影响其他功能

### 5.7 英语单词 PK
- 右上角 PK 按钮进入
- 大模型生成英语选择题（英译中 / 中译英）
- 每日 22:00 自动结算，答对更多者宠物 +20 愉悦度
- 答错不扣愉悦度，仅记录统计
- 缓存机制：未回答前不重复调用 API

### 5.8 便利贴
- 小金毛 3 张：英语一、数学一、408（随机一门）
- 小白 3 张：英语一、数学一、专业课（自动控制/现代控制随机一门）
- 每 12 小时自动刷新一次
- 生成失败自动重试 3 次
- 点击后不主动调用 API，节省额度
- 桌宠答题、便利贴、英语 PK 共享最近内容缓存，尽量避免重复

### 5.9 Todo List
- 支持添加、删除、完成换积分
- 完成需要二次确认，不可撤销
- 任务完成后保留在列表中，可手动删除
- 长文本自动换行完整显示，不再出现省略号
- 两人互相可见对方 Todo 时长，但只能编辑自己的
- 桌宠弹窗与背景热区点击打开的是同一个 Todo 弹窗

---

## 6. 部署与运维

### 6.1 本地开发

```bash
# 一键启动（后端 + 浏览器）
python start.py

# 或分别启动
npm run dev        # 后端
npm run dev:client # 前端
```

### 6.2 生产部署（当前流程）

由于服务器上 `git pull origin main` 因 Gitee HTTPS 凭证问题失败，当前采用本地 `git bundle` + Paramiko 上传到服务器部署。

```bash
# 1. 本地提交并推送
npm run build
git add -A
git commit -m "xxx"
git push gitee main

# 2. 运行本地部署脚本（已配置服务器 root 密码）
# 依赖：.deploy-venv 中已安装 paramiko
# 脚本：.deploy-now.py（未入仓库，含敏感信息）
cd "/c/Users/zhngyan/Desktop/线上自习室"
"./.deploy-venv/Scripts/python.exe" .deploy-now.py

# 3. 脚本会自动：
#    - 创建 git bundle 并上传到服务器 /tmp/deploy.bundle
#    - 在 /opt/app 执行 git pull /tmp/deploy.bundle main
#    - 执行 npm run build
#    - 更新 /opt/app/server/.env 中的 KIMI_API_KEY
#    - pm2 restart online-study-room --update-env
#    - curl 健康检查 /api/health
```

> 注意：`client/dist/` 在 `.gitignore` 中，任何前端修改都必须在服务器执行 `npm run build` 才能生效。

### 6.3 常用服务器命令

```bash
# 查看服务状态
pm2 status
pm2 logs online-study-room
pm2 logs online-study-room --err

# 重启/停止/启动
pm2 restart online-study-room --update-env
pm2 stop online-study-room
pm2 start online-study-room

# 查看进程环境变量
pm2 env online-study-room

# 查看数据库
sqlite3 /var/lib/online-study-room/app.sqlite

# 清空便利贴（强制重新生成）
sqlite3 /var/lib/online-study-room/app.sqlite "DELETE FROM sticky_notes;"
pm2 restart online-study-room --update-env

# 重置桌宠状态（示例：愉悦度设为 100）
sqlite3 /var/lib/online-study-room/app.sqlite "UPDATE pet_states SET happiness=100, runaway=0, runaway_since=NULL, updated_at=datetime('now');"

# 重置不负责任标记
sqlite3 /var/lib/online-study-room/app.sqlite "UPDATE user_pet_inventory SET runaway_marks=0;"

# 清空桌宠答题缓存（强制重新出题）
sqlite3 /var/lib/online-study-room/app.sqlite "DELETE FROM pet_quiz_state;"
```

### 6.4 环境变量说明（server/.env）

```bash
# 必填
JWT_SECRET=xxx                              # 生产环境必须改为强随机字符串
PRESET_USER_A_PASSWORD=hwy041203            # 小金毛密码
PRESET_USER_B_PASSWORD=zy041016             # 小白密码

# 可选
KIMI_API_KEY=sk-xxx                         # 大模型 Key
DB_PATH=/var/lib/online-study-room/app.sqlite
NODE_ENV=production
PORT=3000
```

> 生产环境启动时如果 `JWT_SECRET` 或密码仍是占位值，服务会拒绝启动。

---

## 7. 数据库关键表

| 表名 | 用途 |
|------|------|
| `users` | 用户信息、密码 hash |
| `todos` | Todo 任务 |
| `study_daily` | 每日学习时长（含 `points_awarded_hours`） |
| `shared_kv` | 共享倒计时、留言、迁移标记 |
| `pet_states` | 桌宠状态（愉悦度、进食、离家出走等） |
| `user_pet_inventory` | 用户积分、不负责任标记 |
| `sticky_notes` | 便利贴内容 |
| `pet_quiz_state` | 桌宠答题状态（持久化 2 小时冷却） |
| `english_quiz_daily_stats` | 英语 PK 每日统计 |
| `english_quiz_daily_rewards` | 英语 PK 奖励发放记录 |

---

## 8. 素材清单

### 8.1 背景
- `client/public/assets/user/bg1.png` ~ `bg11.png`
- 已加入 bg6~bg11
- 蒙版：`dogsLeft_hotspotsN.svg`、`dogsRight_hotspotsN.svg`（N=1~11）

### 8.2 桌宠 GIF
- 位于 `client/public/assets/user/pets/`（根据代码实际路径）
- 原始素材：`小白桌宠素材/`、`小金毛桌宠素材/`
- 愉悦度 GIF：例如 `white_happy_1.1.gif`、`golden_happy_5.4.gif` 等
- 进食 GIF：`food_pingguo.gif`、`food_bingqilin.gif` 等
- 头像：`white_avatar.png`、`golden_avatar.png`

> 若后续补充新 GIF，按命名规则放入 `client/public/assets/user/pets/` 并更新对应代码映射。

---

## 9. 最近改动记录

| 时间 | 改动 | 文件 |
|------|------|------|
| 本次会话 | 学习计时持久化到 localStorage，刷新/跨天不丢时长，每 10s 上报 | `client/src/pages/RoomPage.vue` |
| 本次会话 | 学习积分改为每满 1 小时 +1 分，清空历史小数积分 | `server/src/index.js`、`server/src/db/database.js` |
| 本次会话 | 桌宠答题移除英语科目，只保留数学 + 专业课 | `server/src/index.js` |
| 本次会话 | 桌宠答题状态持久化到数据库，2 小时冷却不丢 | `server/src/index.js`、`server/src/db/database.js` |
| 本次会话 | 桌宠答题、便利贴、英语 PK 共享最近内容缓存去重 | `server/src/index.js` |
| 本次会话 | 小白便利贴/桌宠答题改为英语一、数学一 | `server/src/index.js` |
| 本次会话 | Todo List 长文本自动换行完整显示 | `client/src/pages/RoomPage.vue` |
| 本次会话 | bg8/9/10 特效减少：bg9 去掉蝴蝶改为海鸥 + 浮尘 | `client/src/components/AnimatedBackground.vue` |
| 之前 | 集成 bg6~bg11、桌宠系统、英语 PK、便利贴、留言箱未读红点等 | 多处 |

---

## 10. 已知问题与注意事项

1. **构建产物在服务器生成**：`client/dist/` 被 `.gitignore` 忽略，任何前端修改都必须在服务器执行 `npm run build` 才能生效。
2. **PM2 环境变量缓存**：修改 `/opt/app/server/.env` 后必须用 `pm2 restart online-study-room --update-env` 才能生效。
3. **Kimi API 额度**：便利贴每 12 小时自动刷新、答题/PK 手动触发，注意监控额度。
4. **SQLite 并发**：当前使用 better-sqlite3，写入时有锁，双人同时操作通常不会冲突，但极端并发可能排队。
5. **背景特效性能**：低端设备（尤其老平板）可开启系统“减少动态效果”或调低特效层数。
6. **离家出走标记**：目前不可消除，每离家出走一次 +1，显示在对应用户的 Todo 弹窗里。
7. **素材路径**：所有新增素材必须放在 `client/public/assets/user/` 下，并通过相对路径 `/assets/user/...` 引用。
8. **凭证泄露风险**：root 密码和 Kimi API Key 曾出现在 Gitee Git 历史，建议尽快重置/轮换。
9. **Gitee 拉取凭证**：服务器目前无法直接 `git pull origin main`，需通过 `.deploy-now.py` 的 git bundle 方式部署，或给服务器配置 Gitee SSH key / HTTPS 凭证。

---

## 11. 紧急联系与恢复

如果服务完全无法访问：

```bash
ssh root@120.55.183.89
# 密码：tbszxy123!

cd /opt/app
# 方式一：若服务器已恢复 git 凭证，直接拉取
# git pull origin main
# npm run build
# pm2 restart online-study-room --update-env

# 方式二：在本地运行 .deploy-now.py 上传 bundle 部署
```

如果数据库损坏，可重置（会清空数据）：

```bash
pm2 stop online-study-room
rm /var/lib/online-study-room/app.sqlite
cd /opt/app && npm run build
pm2 restart online-study-room --update-env
```

---

## 12. 备忘

- 用户女友使用平板登录，后续 UI 改动需重点验证 768px~1024px 区间。
- 考研日期默认：2026-12-20。
- 剩余备考时间：约 5 个月。
- 服务器续费、安全组、备案等需自行关注阿里云控制台。
- 部署脚本 `.deploy-now.py` 和 `.deploy-venv/` 仅存在于本地，不要提交到 Git 仓库。

---

> 文档结束。如有任何项目交接、服务器或代码问题，优先查看本文件。
